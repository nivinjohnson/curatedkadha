# Copy these keys into Streamlit Cloud App Settings -> Secrets,
# or into local .streamlit/secrets.toml (which is gitignored).

STOCK_PAGE_USERNAME = "your-email@example.com"
STOCK_PAGE_PASSWORD = "your-strong-password"

# Optional: checkout email settings used by cart checkout
# SHOP_SMTP_HOST = "smtp.gmail.com"
# SHOP_SMTP_PORT = "587"
# SHOP_SMTP_USER = "your-smtp-user"
# SHOP_SMTP_PASSWORD = "your-smtp-password"
# SHOP_FROM_EMAIL = "your-from-email@example.com"
# SHOP_SMTP_USE_TLS = "true"
